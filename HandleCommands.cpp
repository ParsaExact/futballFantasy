#include "Globalstuff.hpp"
#include "HandleCommands.hpp"
#include"Input.cpp"
void HandleCommands::get_commands()
{
    string line;
    vector<string> league_info = read_from_file(LEAUGE_ADDRESS);
    while (getline(cin, line))
    {
                
    }
}
